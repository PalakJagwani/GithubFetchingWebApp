# GithubFetchingWebApp
GithubFetchingWebApp is a project in which I have made use of HTML, CSS and Javascript in order to fetch users on github.
I have made use of the fetch API provided by Javascript.
